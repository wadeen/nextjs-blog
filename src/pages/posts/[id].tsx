const post = () => {
  return (
    <div>
      {/* <p>タイトル：{post.title}</p>
      <p>カテゴリ名：{post.category.name}</p>
      <p>作成日時：{post.date}</p>
      <p>更新日時：{post.update}</p>
      <p>アイキャッチ画像：{post.eyecatch}</p>
      <p>投稿内容：<p dangerouslySetInnerHTML={{__html: post.content}}></p></p> */}
    </div>
  );
}

export default post;